# Static site (Vercel)

Portfolio page for **[Batch-Scoring-Pipeline](https://github.com/vahdetkaratas/Batch-Scoring-Pipeline)**.

**Assets:** `favicon.svg` (VK monogram on `#1a1a1f`, portfolio-aligned), `shell.css` (layout-shell: sidebar + rail + footer), `project.css` (Batch Scoring content: Poppins + Courier Prime).  
**Deploy:** Vercel → import repo → **Root Directory:** `vercel_static` → Deploy.  
**Domain / SEO:** `index.html` uses `https://batch-scoring.vahdetkaratas.com/` for `canonical` and `og:url`; fork/custom domain → update those tags (see `docs/PUBLISHING.md`).

More: [docs/PUBLISHING.md](../docs/PUBLISHING.md).
